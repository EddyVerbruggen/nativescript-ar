import { ARAddTextOptions } from "../../ar-common";
import { ARCommonNode } from "./arcommon";
export declare class ARText extends ARCommonNode {
    static create(options: ARAddTextOptions): ARText;
}
