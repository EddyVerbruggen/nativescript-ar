import { ARAddGeometryOptions } from "../../ar-common";
import { ARCommonNode } from "./arcommon";
export declare abstract class ARCommonGeometryNode extends ARCommonNode {
    constructor(options: ARAddGeometryOptions, node: SCNNode);
}
